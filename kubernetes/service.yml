apiVersion: v1
kind: Service

metadata:
  name: student-app-service
  namespace: student-ns

spec:
  type: NodePort

  selector:
    app: student-app

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080